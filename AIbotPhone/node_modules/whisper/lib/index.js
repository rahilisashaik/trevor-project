(function(){
  var ref$, allTasks, task, configure;
  ref$ = require('./tasks'), allTasks = ref$.allTasks, task = ref$.task;
  configure = require('./environment').configure;
  module.exports = {
    allTasks: allTasks,
    task: task,
    configure: configure
  };
}).call(this);
